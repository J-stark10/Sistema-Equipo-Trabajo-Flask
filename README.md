# Flask App Factory + Blueprints + Authentification

Este proyecto es una aplicación web construida con Flask usando el patrón App Factory y Blueprints. Está organizada para manejar al menos un módulo funcional de `miembros` con CRUD básico, además de una estructura para posibles otras secciones como `tareas`. Y ademas un login funcional.
